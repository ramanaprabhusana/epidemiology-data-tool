# Copy to secrets.toml locally only (never commit secrets.toml).
# On Streamlit Community Cloud: App → Settings → Secrets (TOML in the UI).
#
# Example shape when you add keys:
# [api_keys]
# SOME_SERVICE = "your-key-here"
